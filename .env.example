# Copy to .env for local dev, or set these as environment variables on Render.
SHOWALL_SECRET=your-shared-admin-secret
TELEGRAM_BOT_TOKEN=123456:telegram-bot-token
# Full service-account JSON on ONE line (Render). Locally you can use serviceAccount.json instead.
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"edgy-store", "...":"..."}
# Optional overrides:
# INSTALL_URL=https://ipa.authtool.app/api/install/xxxx
# API_BASE=https://ske2.onrender.com
